# RLMolLM Core Requirements (Python 3.11.11)
# This matches the core dependencies in pyproject.toml
# For optional dependencies (viz, api, test), use: pip install -e ".[viz]"

# Deep Learning & AI Frameworks
torch>=2.4.0
transformers>=4.50.0
numpy>=1.24.3

# Chemistry & Molecular Processing
rdkit>=2024.3.3
admet-ai>=1.3.1

# Data Science & Core Analysis
pandas>=2.1.4
scipy>=1.14.0

# Utilities
tqdm>=4.66.4

# Graph Neural Networks (for LiTEN ADMET)
torch-geometric>=2.5.0

# ============================================================================
# Optional Dependencies (not in core requirements.txt)
# ============================================================================
# To install optional dependencies:
#   pip install -e ".[viz]"      # matplotlib, seaborn, umap-learn
#   pip install -e ".[api]"      # fastapi, uvicorn, pydantic
#   pip install -e ".[test]"     # pytest, pytest-cov
#   pip install -e ".[all]"      # everything
# ============================================================================ 